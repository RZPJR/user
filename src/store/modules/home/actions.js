import http from "../../../services/http";

const actions = {
  function: async ({ commit, dispatch }, payload) => {
    //write your code here, use http to hit api with bearer
  },
};

export default actions;

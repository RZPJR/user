---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-bp
  labels:
    app: fe-bp
  namespace: fe-bp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fe-bp
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: fe-bp
    spec:
      imagePullSecrets:
      - name: registry
      containers:
      - name: fe-bp
        image: git.edenfarm.id:5050/project-version3/erp-services/module-boilerplate/dev:<VERSION>
        ports:
          - name: http-port
            containerPort: 8080
        imagePullPolicy: Always
        resources:
          requests:
            memory: 150Mi
            cpu: 100m
          limits:
            memory: 500Mi
            cpu: 300m
        volumeMounts:
        - name: tz-config
          mountPath: /etc/localtime
      volumes:
        - name: tz-config
          hostPath:
            path: /usr/share/zoneinfo/Asia/Jakarta
            type: File
---
apiVersion: v1
kind: Service
metadata:
  name: svc-fe-bp
  namespace: fe-bp
  labels:
    app: fe-bp
spec:
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: http-port
  selector:
    app: fe-bp
---
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: hpa-fe-bp
  namespace: fe-bp
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: fe-bp
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 75
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 75
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: fe-bp
  namespace: fe-bp
spec:
  dnsNames:
    - fe-bp.edenfarm.tech
  secretName: fe-bp-tls
  issuerRef:
    name: letsencrypt-cluster-issuer
    kind: ClusterIssuer
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: fe-bp
  annotations:
    kubernetes.io/ingress.class: "nginx"
  name: fe-bp
spec:
  tls:
  - hosts:
    - fe-bp.edenfarm.tech
    secretName: fe-bp-tls
  rules:
  - host: fe-bp.edenfarm.tech
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: svc-fe-bp
            port:
              number: 80
